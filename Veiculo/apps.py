from django.apps import AppConfig

class VeiculoConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Veiculo'  # Certifique-se que o nome aqui corresponde à pasta do app